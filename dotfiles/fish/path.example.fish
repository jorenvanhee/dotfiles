# https://fishshell.com/docs/current/cmds/fish_add_path.html
#
# I'm using the --global flag because otherwiser a variable with a machine
# specific path will be added to the fish_variables file (which is checked
# into git).
#
# fish_add_path --global ~/.composer/vendor/bin
